apiVersion: v1
kind: Service
metadata:
  name: muchtodo-backend-service
  namespace: muchtodo
spec:
  selector:
    app: muchtodo-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
